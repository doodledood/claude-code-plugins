---
description: Implement features with small increments and quality gates (typecheck, test, lint).
allowed-tools: ["Task", "Read", "Glob", "Grep"]
argument-hint: [task description with context]
---

Use the code-implementor agent to implement the following task: $ARGUMENTS
